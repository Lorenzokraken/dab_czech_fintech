bundle:
  name: czech-fintech

variables:
  cluster_id:
    default: "0401-144924-a6ruxrqa"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405607744677493.13.azuredatabricks.net

resources:
  jobs:
    bronze_ingestion:
      name: "[dev] Bronze Ingestion"
      tasks:
        - task_key: bronze_static
          existing_cluster_id: ${var.cluster_id}
          notebook_task:
            notebook_path: ./notebooks/01_bronze.py
            source: WORKSPACE
